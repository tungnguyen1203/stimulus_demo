<p id="notice"><%= notice %></p>

<h1>Animals</h1>
<%= link_to 'New Animal', new_animal_path %>
<div data-controller='animal' data-action='scroll@window->animal#scroll'>
  <div data-animal-target="entries">
     <%= render "list_animal"%>
  <br>
  </div>
  <div data-animal-target="pagination">
    <%= paginate @animals %>
  </div>
  
</div>
